interface:
  display_name: "Apply Code Conventions"
  short_description: "Apply effective rules to authorized Unity edits"
  default_prompt: "Use $code-convention-apply after authorizing a Unity code change to apply local-first conventions within that scope."
policy:
  allow_implicit_invocation: true
